package com.ekenya.rnd.onboarding.base
import com.ekenya.rnd.onboarding.dataonboarding.api.ApiHelper


class ViewModelFactory(private val apiHelper: ApiHelper) /*: ViewModelProvider.Factory*/ {

/*override fun <T : ViewModel?> create(modelClass: Class<T>): T {
    if (modelClass.isAssignableFrom(TestMainViewModel::class.java)) {
        return TestMainViewModel(MainRepository()) as T
    }
    throw IllegalArgumentException("Unknown class name")
}*/

}